name: exam_guard
description: "AI-powered Government Exam Timetable Conflict Resolver - INVICTUS 1 | HAT-059"

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_animate: ^4.5.0
  google_fonts: ^6.2.1
  http: ^1.2.0
  shared_preferences: ^2.2.2
  url_launcher: ^6.3.0
  intl: ^0.19.0
  fl_chart: ^0.68.0
  shimmer: ^3.0.0
  confetti: ^0.7.0
  provider: ^6.1.2
  uuid: ^4.3.3
  table_calendar: ^3.1.0
  percent_indicator: ^4.2.3
  animated_text_kit: ^4.2.2
  flutter_staggered_animations: ^1.1.1
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
